// import { useMemo } from 'react';

const Address = ({ address }) => {
  console.log(address)

  return (
    <div>

    </div>
  )
}

Address.defaultProps = {
  address: {}
}

export default Address;